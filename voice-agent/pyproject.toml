[project]
name = "voice-agent"
version = "0.2.0"
description = "OpenClaw voice AI agent with Redis memory"
requires-python = ">=3.13"
dependencies = [
    "livekit-agents[openai,silero]~=1.3",
    "python-dotenv",
    "httpx>=0.27.0",
    "redis>=5.0.0",
]
